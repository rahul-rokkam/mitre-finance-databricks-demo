bundle:
  name: financial-command-center

sync:
  include:
    - .build

artifacts:
  default:
    build: uv run apx build

resources:
  apps:
    financial-command-center-app:
      name: "financial-command-center"
      description: "financial-command-center created with apx"
      source_code_path: ./.build

targets:
  dev:
    mode: development
    default: true